import styled from "styled-components";

export const StyledTrashTooltip = styled.div`
    .BS-tooltip-trash--title {
        font-size: ${(p) => p.theme.typography.fontSize.p12};
        line-height: 14px;
        text-align: center;
    }
`;
